{
  "name": "forme-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web",
    "shared"
  ],
  "scripts": {
    "web": "npm run --prefix web",
    "web:dev": "npm run dev --prefix web",
    "web:build": "npm run build --prefix web",
    "web:start": "npm run start --prefix web",
    "ios:open": "open ios/ForMe.xcodeproj"
  }
}
